{
  "name": "Demo IA",
  "version": "1.0.0",
  "description": "Demo de integração com IA usando OpenRouter, MCP (Model Context Protocol) e Vercel AI SDK — inclui extração de notas fiscais por imagem, servidor MCP para cadastro de despesas e exemplos de tools/RAG.",
  "type": "module",
  "scripts": {
    "dev": "json-server db.json --static .",
    "api": "bun mcp/main.api.ts",
    "chat": "cd chat && bun run dev",
    "start": "concurrently \"bun run dev\" \"bun run api\" \"bun run chat\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4",
    "@openrouter/ai-sdk-provider": "^2.3.1",
    "@types/node": "^25.5.0",
    "ai": "^6.0.116",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "json-server": "^1.0.0-beta.14",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "concurrently": "^9.2.1"
  }
}
